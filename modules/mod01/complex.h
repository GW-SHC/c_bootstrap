#ifndef COMPLEX_H
#define COMPLEX_H

/**
 * This header file defines a struct representing a complex number
 * Complex numbers have a real component and an imaginary component, represented
 * here with two doubles
 */

struct complex {
    double real;
    double imaginary;
};

#endif
